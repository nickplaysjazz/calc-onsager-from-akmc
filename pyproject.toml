[project]
name = "calc-onsager-from-akmc"
version = "0.1.0"
description = "A simple tool to calculate Onsager coefficients from FCC AKMC simulations defined with pairwise nearest-neighbor interactions."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pathlib>=1.0.1",
]
